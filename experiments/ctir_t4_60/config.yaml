experiment_id: EXP-CTIR-T4-DYNAMIC-60
name: T4-CTIR-Dynamic-60
continual_start_step: 900
task: Puzzle
model_start: checkpoints/Qwen3-VL-4B/GRPO-CL/training/We-Math2
hardware:
  physical_gpus: [0, 1, 2, 3]
  world_size: 4
training:
  max_steps: 60
  save_steps: 10
  logging_steps: 1
  num_generations: 8
  per_device_train_batch_size: 8
  gradient_accumulation_steps: 4
  learning_rate: 5.0e-6
  weight_decay: 0.1
  warmup_ratio: 0.1
  lr_scheduler_type: cosine
  bf16: true
  tf32: true
  deepspeed: scripts/zero3_offload.json
  max_completion_length: 2048
  temperature: 0.8
  top_p: 1.0
  top_k: 0
  epsilon: 0.2
  epsilon_high: 0.27
  seed: 42
  data_seed: null
  full_determinism: false
ctir:
  protected_layers: [9, 26]
  protected_modules: [q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj]
  tangent_rank: 8
  raw_dominant_rank: 8
  raw_delta_is_truncated: false
  refresh_interval: 5
  refresh_steps: [0, 5, 10, 15, 20, 25, 30, 35, 40, 45, 50, 55]
  beta_candidates: [0.0, 0.25, 0.5, 0.75, 1.0]
  new_descent_ratio: 0.90
  probe_count: 32
  probe_seed: 142
  probe_target: full_text_only_thought
  spectrum_object: complete FP32 AdamW master-parameter delta
  spectrum_logging: per-step Householder orthogonality-plus-Frobenius certificate; exact FP64 SVD only in EXP-CTIR-002 gate
artifacts:
  probes: experiments/ctir_t4_60/probes/navigation_probes.json
  step_metrics: experiments/ctir_t4_60/logs/step_metrics.jsonl
  tangent_refresh: experiments/ctir_t4_60/logs/tangent_refresh.jsonl
  checkpoints: checkpoints/Qwen3-VL-4B/CTIR-T4-60/training/Puzzle
correctness:
  scheduler_max_steps: 60
  warmup_ratio: 0.1
  debug_stop_after_optimizer_steps: 2
  measured_optimizer_step: 2
  note: step 1 is the formal-schedule zero-LR warmup update and is not used as correctness evidence
